name = "tracker"
version = "0.1.0"
defaultTargets = ["tracker"]

[[lean_lib]]
name = "Tracker"

[[lean_exe]]
name = "tracker"
root = "Main"
# The checker imports the target project's modules and may run their initializers.
supportInterpreter = true
