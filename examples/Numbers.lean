import Numbers.Parity
import Numbers.Odd
import Numbers.Wrong

/-!
# Natural numbers

The example library: parity of natural numbers, a task on odd numbers in progress, and a refuted
proposal. Every module under it has a plan file at the same path under `tracker/`.
-/
