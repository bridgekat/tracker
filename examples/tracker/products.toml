# Blocked: depends on an open node of the odd-numbers task, so `tracker ready` leaves it out
# until that task is proved.
kind = "task"
title = "Products"
module = "Example.Products"
namespace = "Example"

[[node]]
id = "IsOdd.mul"
kind = "theorem"
desc = 'The product of two odd numbers is odd.'
deps = ["IsOdd", "IsOdd.add_odd"]
