# The task in progress: one node is misfiled, one is stated with `sorry`, one is not written yet.
# Every outside dependency is proved, so `tracker ready` lists it. The `desc` is kept although
# the module already has a doc comment, so that `lint` reports it as superseded.
namespace = "Numbers"
desc = '''
Prove the odd-number lemmas from the parity module. `IsEven.add_one_odd` does the same in the
other direction and is the model to follow.
'''

# Lives in Numbers.Parity, not here: `lint` reports the placement mismatch.
[[node]]
id = "isOdd_one"

# Stated with `sorry` and documented, so only the suggested dependencies remain useful.
[[node]]
id = "IsOdd.add_one_even"
deps = ["IsOdd", "IsEven"]

[[node]]
id = "IsOdd.add_odd"
kind = "theorem"
desc = 'The sum of two odd numbers is even.'
deps = ["IsOdd", "IsEven", "IsOdd.add_one_even"]
source = "Textbook, Proposition 1.2"
