# The closure of a convex function. Everything here is finished: kinds, doc comments and real
# dependencies are read from the library, so the nodes keep only their id.
kind = "module"
title = "Closure of a convex function"
module = "Tdaf.Analysis.Convex.Closure"
namespace = "Tdaf.ConvexAnalysis"

[[node]]
id = "lscHull"

[[node]]
id = "clFn"

# These three have no doc comment in the library, so the plan keeps their descriptions.
[[node]]
id = "lscHull_le"
desc = 'The lsc hull lies below the function.'

[[node]]
id = "clFn_le"
desc = 'cl f ≤ f.'

[[node]]
id = "closedFn_clFn"
desc = 'cl f is closed.'

[[node]]
id = "convexFn_clFn"

# Deliberately filed in the wrong group: it lives in Duality/Conjugate. `tracker lint` reports
# the placement mismatch from the module the environment records for it.
[[node]]
id = "biconj_le_clFn"
