# An open task: nothing here exists yet, so its state comes from the suggestions alone. Every
# outside dependency is proved, so `tracker ready` lists it.
kind = "task"
title = "Conjugates of improper functions"
module = "Tdaf.Analysis.Convex.Duality.Improper"
namespace = "Tdaf.ConvexAnalysis"
notes = '''
Small task to close the gap between conj_bot (the constant -∞) and the general improper case.
'''

[[node]]
id = "conj_eq_top_of_exists_eq_bot"
kind = "theorem"
desc = 'If f takes the value -∞ anywhere, then f* is identically +∞.'
deps = ["conj", "conj_bot"]

[[node]]
id = "biconj_eq_bot_of_exists_eq_bot"
kind = "theorem"
desc = 'If f takes the value -∞ anywhere, then f** is identically -∞.'
deps = ["biconj", "conj_eq_top_of_exists_eq_bot", "conj_top"]
